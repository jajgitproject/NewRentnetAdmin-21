// @ts-nocheck
import { formatDate } from '@angular/common';
export class StopsPopupDropDown {
   stopsPopupID: number;
   stopsPopup: string;

  constructor(stopsPopupDropDown) {
    {
       this.stopsPopupID = stopsPopupDropDown.stopsPopupID || -1;
       this.stopsPopup = stopsPopupDropDown.stopsPopup || '';
    }
  }
  
}

